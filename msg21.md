مرحبا،

تحديث بخصوص الخطأ `Call to undefined method App\Models\User::courseProgress()`:

- تم تحديد سبب الخطأ وهو عدم وجود علاقة `courseProgress` في نموذج `User`.
- تم إضافة العلاقة المطلوبة إلى ملف `app/Models/User.php`.

يجب أن يكون الخطأ قد تم حله الآن.

شكرا.
