مرحبا،

تحديث بخصوص مشكلة تداخل رأس الصفحة مع شريط التنقل:

- تم تعديل ملف `resources/views/layouts/public.blade.php`.
- تم إضافة الفئة `pt-8` (padding-top) إلى وسم `<main>` في ملف `public.blade.php` لحل مشكلة التداخل مع شريط التنقل الثابت.

يرجى التحقق من `/portfolio` والتأكد من حل المشكلة.

شكرا.
